// use the string literal `string ${property of object} ${variable} string`
// print it to the console: console.log(`string ${property of object} ${variable} string`);

// example code below
// run this file in the terminal to see it print to the console. 

const name = "Muhammad";
const age = 29;
console.log(`My name is ${name} and I am ${age} years old.`);