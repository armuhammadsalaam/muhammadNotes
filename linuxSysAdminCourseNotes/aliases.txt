Aliases -

Create an alias to shorten and represent commands used often
Plz refer to man alias for detailed functionality
	alias name=value	Fix typos; make behave as another
				OS
	ex:
	alias grpe=grep		Alias only exists per session;
				persist changes in bash profile
	unalias			deletes aliases;

