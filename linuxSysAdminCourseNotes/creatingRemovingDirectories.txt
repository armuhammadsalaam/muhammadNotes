Creating and Removing Directories-

	mkdir	directoryName	Creates and declares name passed in
	rm	directoryName	Removes directory name passed in
	mkdir -p first/second/third	Create nested directories
					-p option declares parent directory
	rm -rf directory	Recursively removes directory; no recovery
	.	declares current directory
	..	declares parent directory
	./command-in-current-directory
